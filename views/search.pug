doctype html
html
  head
    meta(charset='UTF-8')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')
    link(rel='stylesheet' href='/css/style.css')
    title Platform | #{title}

  body
    //NAV
    include _header
    
    //- h1 this is h1
    //- each fi in final
    //-   h1= fi

    section.search 
      .search-box 
        form.search-form(action="") 
          .div 
            input#search-input(type="text" placeholder="Type something")
            input#search-submit(type="submit" value="Search")

    h1= data
    each ne in newData
      h2 #{ne}
      hr